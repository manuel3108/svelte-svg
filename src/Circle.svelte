<Ellipse centerX={centerX} centerY={centerY} radiusX={radius} radiusY={radius} />

<script>
    import Ellipse from './Ellipse.svelte'

    export let centerX;
    export let centerY;
    export let radius;
</script>